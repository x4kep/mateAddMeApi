'use strict';

 //ManyToMany
//  SELECT * FROM gameprofilegamemode
//  INNER JOIN gameprofile ON gameprofilegamemode.gameProfileId = gameprofile.id
//  INNER JOIN gamemode ON gameprofilegamemode.gameModeId = gamemode.id

module.exports = function(Gamemode) {

};
